# INTENT

CTF初学者向けSQLi教材。目的: 座学でなく手を動かして「なぜ危険か」体で覚えさせる。

## なぜ3問・各1コンテナか

当初「1コンテナに複数問」案も検討したが、ユーザー判断で却下 → 1問1アプリ1DBに統一。
理由: 問題間の依存ゼロにして、詰まった学習者が他問に影響されず切り分けできる。docker-compose上も
`docker compose logs app-01` 等で問題単位のデバッグがしやすい。

## なぜ「現場のミス」ベースか

CTF特有の不自然な罠(わざとらしいエンドポイント名、ヒントのための冗長コメント)は避ける方針。
初学者が実務コードを読んだ時に「あ、これ危ない」と気づける転移可能な知識にしたいため、各問に
実際にありがちな実装ミスを1つずつ対応させた:

- 01: パスワード平文保存+SQL直接比較。「今どき平文はない」と思われがちだが、レガシー/社内ツールで
  実際に頻発するアンチパターン。SQLi単体でなく「平文保存+文字列結合」の組み合わせで一発アウトになる
  典型を体感させる
- 02: 検索機能のLIKE句を素朴に文字列結合 + verboseエラー出力(デバッグ設定の本番残し)
- 03: 「数値だから安全」という思い込み → 型バリデーション省略のblind injection

## ソース公開方針

白箱(ソースコード公開)CTFとする。実務ではコードレビューでSQLi気づくのが理想 → ソースを読んで
「危ない書き方」を目で見つける訓練も兼ねる。black-boxのpayload探索力だけでなく、コードレビュー力も養う。

## DB非公開の理由

mysqlコンテナはhost portを公開しない。「本番DBを直接外部に晒さない」という当たり前の運用原則を
環境自体でも体現し、攻撃はアプリ層経由のみに限定させる。
