# INTENT

SQLインジェクション未経験者が、SQL文へ入力が混ざる感覚を手作業で掴むための教材。
知識量や反復作業を競うCTFではない。

## 難易度

全問を01と同じ入門難易度に揃える。1問で新しく判断させる要素は1つ、payloadは短く、
前問の考え方を再利用できる構成にする。SQL専用関数の暗記、複数の回避策の同時適用、
flag全文の総当たりは扱わない。

1. クォートを閉じ、コメントでpassword条件を消す。
2. UNIONで別テーブルの値を検索結果へ混ぜる。
3. 数値条件へ`OR 1=1`を追加して全件表示する。
4. 大文字だけを見るフィルタを小文字のSQLキーワードで通過する。
5. 価格検索を選び、03と同じ`OR 1=1`を使う。
6. true/falseへ1桁の質問を行い、判明したcodeでflagを受け取る。

03以降は新しい抽出技法の網羅より、入力と実行SQLの対応を理解できることを優先する。
問題文には必要なテーブル構成と最初の観察点を明記し、ヒントは「SQL確認」「使う構文」
「解答」の順にする。

## 配布範囲

flag実値は`.env`だけに置く。DBへの保存は各問題の`db/02-flag.sh`が起動時に行う。
リポジトリのコード、schema、問題README、`.env.example`は公開できる。

運営がホストする場合は運営だけが`.env`を持つ。参加者が自分で起動する場合は
`.env.example`を`.env`へコピーし、動作確認用のflagを使う。
