[project]
name = "ctf-02-or-listing"
version = "0.1.0"
description = "Beginner SQL injection challenge using an OR condition"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "flask>=3.1.3",
    "pymysql>=1.2.0",
]
