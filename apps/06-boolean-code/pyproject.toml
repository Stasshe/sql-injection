[project]
name = "ctf-06-boolean-code"
version = "0.1.0"
description = "Beginner boolean-blind SQL injection challenge using a one-digit code"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "flask>=3.1.3",
    "pymysql>=1.2.0",
]
