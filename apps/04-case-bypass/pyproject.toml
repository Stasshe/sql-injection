[project]
name = "ctf-04-case-bypass"
version = "0.1.0"
description = "Beginner SQL injection challenge using a case-sensitive filter bypass"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "flask>=3.1.3",
    "pymysql>=1.2.0",
]
